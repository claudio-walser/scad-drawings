include <./config.scad>

module fireBasket() {

    module partsList() {
        // Spindelplatte
        echo(str("50;Feuerkorb Seitlich - Vierkantrohr - 20*20;2;", 200, ";", 20, ";", 20));
    }

    partsList();
}